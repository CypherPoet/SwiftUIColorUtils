import SwiftUI


enum TestConstants {
    
    enum ThemeColors {
        
        enum Group1 {
            static let primary = Color("Theme1-Primary", bundle: .module)
            static let secondary1 = Color("Theme1-Secondary1", bundle: .module)
            static let secondary2 = Color("Theme1-Secondary2", bundle: .module)
            static let secondary3 = Color("Theme1-Secondary3", bundle: .module)
            static let pop1 = Color("Theme1-Pop1", bundle: .module)
            static let pop2 = Color("Theme1-Pop2", bundle: .module)
            static let pop3 = Color("Theme1-Pop3", bundle: .module)
        }
        
    }
}
